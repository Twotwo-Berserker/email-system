# 无需授权码的邮件收发

本系统提供两条获得邮箱的途径。**「本域地址」这条路完全不涉及授权码** ——
本文说明它是怎么做到的、代价是什么，以及各部分的部署入口。

| | 绑定外部邮箱 | 领取本域地址 |
|---|---|---|
| 你填什么 | 邮箱地址 + **授权码** | 只填用户名 |
| 邮箱归谁 | 你已有的 QQ / 网易 / Gmail 邮箱 | 本系统域名下的新地址（如 `alice@mail.example.com`） |
| 谁在收信 | 本系统用 IMAP **登录**你的邮箱拉取 | Cloudflare 在域名上接收后**推送**给本系统 |
| 谁在发信 | 用你邮箱的 SMTP 服务器 | 本系统的项目级中继 |
| 代码里的类型 | `IMAP_SMTP` | `CLOUDFLARE` |

两条路可以同时存在，互不影响。「邮箱账户」页会自动判断当前部署能用哪条。

---

## 为什么传统做法绕不开授权码

授权码不是本系统的限制，而是**邮箱服务商**的认证要求：本系统要读你的 QQ 邮箱，
就必须以"一个第三方客户端"的身份登录进去，而 QQ 只认授权码，不认别的东西。

于是唯一的解法是**不去登录任何人的邮箱**：让信自己送到我们这边来。
这正是 Cloudflare Email Routing 提供的机制 —— 收信方从"客户端"变成了"收件服务器"。

```
传统方式（需要授权码）              本系统的方式（不需要授权码）

本系统 ──登录──▶ QQ 邮箱            外部发件人 ──标准 SMTP──▶ 本项目域名的 MX
   ↑  要授权码                        ↓
   └── IMAP 拉取自己的信              Cloudflare 在边缘接收并推送
                                       ↓
                                     本系统（不登录任何邮箱）
```

---

## 一、收信：Cloudflare Email Routing + Email Worker

```
外部发件人
  │ 标准 SMTP 投递（对方完全不需要知道本系统的存在）
  ▼
本项目域名的 MX 记录 ──▶ Cloudflare Email Routing
                              │ 触发邮件处理函数
                              ▼
                        Email Worker（deploy/cloudflare/src/index.js）
                              │ POST + HMAC-SHA256 签名
                              ▼
                        后端 /inbound/cloudflare
                              │ 校验签名 → 解析 MIME → 查地址归属 → 落库
                              ▼
                        用户收件箱
```

**关键点**：整个链路里，本系统从来没有"登录"任何邮箱。QQ 邮箱在这里的角色是
**发件方**，和 Gmail、企业邮、任何一个能发信的服务器没有区别 —— 你不需要持有
发件人的任何凭据。所以这一步没有授权码可填，不是省略了，而是不存在。

### 签名：证明"这封信确实来自我们的 Worker"

`/inbound/cloudflare` 是一个**写接口** —— 谁能调用它，谁就能往用户收件箱里塞邮件。
因此它不接受裸请求，每一条都必须带 `X-Inbound-Timestamp` 与 `X-Inbound-Signature`：

```
签名 = HMAC-SHA256(INBOUND_SHARED_SECRET, timestamp + "." + 请求体原始字节)
```

- 时间戳超出 **5 分钟**窗口即拒绝 —— 拦住重放
- 比较用常量时间，避免通过响应耗时逐字节猜出密钥
- **密钥绝不能写进 `wrangler.toml`**（`[vars]` 的内容会进入部署产物），
  用 `wrangler secret put INBOUND_SHARED_SECRET` 写入

### 退信，而不是丢信

Worker 根据后端返回的 HTTP 状态与 `status` 字段分为三类处置，这个区分是整个
链路可运维的前提：

| 后端返回 | Worker 的动作 | 为什么 |
|---|---|---|
| `UNKNOWN_RECIPIENT` / `DISABLED` | **退信**给发件人 | 地址没人领或已停用。丢信的话，发件人以为送达了，收件人永远等不到 |
| 401 / 400 / 413 / 503 | **退信** | 密钥不一致、报文畸形、超限、后端未启用 —— 重试一万次还是同样的结果 |
| 网络不可达 / 500 | **抛出** | 暂时性故障。Cloudflare 会按自己的策略重试，此时丢信或退信都是错的 |

### 部署

完整步骤（开通 Email Routing、部署 Worker、配 Catch-all 路由、验证）见
**[deploy/cloudflare/README.md](deploy/cloudflare/README.md)**。

后端侧只需要三个环境变量：`INBOUND_ENABLED`、`INBOUND_SHARED_SECRET`、`INBOUND_DOMAINS`。

---

## 二、发信：项目级中继（Resend / SMTP）

Cloudflare Email Routing **只负责收信**，不提供发信服务 —— 这很常见，
它的定位就是"把信接住"，而不是"替你发信"。

所以本域地址要能发信，必须额外配一条**以本项目域名为身份**的发信通道。
注意这条通道用的是**项目自己的凭据**（一个 API Key 或一个 SMTP 账号），
与"用户的 QQ 邮箱授权码"完全是两回事。

### 两条通道

在 `.env` 里用 `OUTBOUND_TRANSPORT` 选择：

| 取值 | 通道 | 需要配什么 | 适用场景 |
|---|---|---|---|
| `none`（默认） | 不发信 | —— | 只想收信（例如收验证码、注册确认信） |
| `resend` | Resend HTTP API | `RESEND_API_KEY` | **推荐**。走 443 端口，不受云主机封禁出站 25/465/587 的影响；无需自建服务器 |
| `smtp` | SMTP 中继 | `RELAY_SMTP_HOST` 等 | 已有自己的邮件服务器，或使用企业邮箱的 SMTP 中继 |

`resend` 之所以走 HTTP 而不是它同样提供的 SMTP 接口：很多云主机默认封禁出站25/465/587，而 443 永远开着。既然有 HTTP API，就没有理由让部署者先去申请解封端口。

### DKIM 由中继方签名，不是本系统签名

本系统投递时**不做 DKIM 签名** —— 签名是发信中继的职责，也是它必须被正确配置的原因：

- **Resend**：需要先在 Resend 后台把你的域名加进去，它会给出几条 DNS 记录（SPF / DKIM），把这些记录加到 Cloudflare DNS 里并等待验证通过。
  **验证通过之前，用这个域名发出的信会被收件方拒收或直接判为垃圾邮件。**
- **自建 SMTP 中继**：由你的邮件服务器负责签名（Postfix + OpenDKIM 等）。

配合第一步在 Cloudflare 上开通 Email Routing 时自动添加的 SPF 记录，
收件方才能确认"这封信确实来自这个域名、且中途没被篡改"。

### 发件人地址是固定的

无论走哪条通道，`From` 都固定为该账户自己的地址：绑定的邮箱账户就用那个邮箱，本域地址就用领到的那个地址。这不做成可配置项 —— Resend 与各大邮箱服务商都会校验 `From` 与身份是否一致，不一致会被拒信。显示名（`displayName`）可以随便改，它不影响这个校验。

### 中继没配好时会怎样

不会被静默忽略：

- **发送前**：如果收件人里有外部地址、而你要用的正是本域地址，系统会直接拦住，并告诉你"本域地址依赖发信中继，当前未配置"。**不会**先落库再在几十秒后异步失败 —— 那样你会在"已发送"里看到一封永远发不出去的信。
- **发送时**：失败原因（含中继服务商返回的原文，如 `domain is not verified`）会写进「已发送」列表的投递状态，同时通过 WebSocket 弹窗告诉你。

---

## 三、SMTP/IMAP 代理：让传统客户端也能用

Web 界面之外，Thunderbird、Outlook、手机邮件 App 也能直接收发本系统的邮件：

```
邮件客户端 ──SMTP/IMAP──▶ 代理（proxy/）──HTTP + JWT──▶ 后端
```

代理**没有数据库、没有自己的用户体系、不持有任何邮箱凭据**。
登录它用的是**本系统自己的账号密码**（换取 JWT），权限判断仍然全部由后端完成。

这也正是不涉及授权码的原因：代理不是"另一个能访问你邮箱的东西"，
它只是 Web 界面的另一种呈现形式。

用法、支持的协议命令、已知限制见 **[proxy/README.md](proxy/README.md)**。

---

## 三者的边界

| 组件 | 负责 | 不负责 |
|---|---|---|
| Cloudflare Email Routing + Worker | 接收发往本项目域名的邮件，签名后转发 | 发信；解析 MIME（解析只写在后端一处，两边各写一份必然在边界条件上分叉） |
| 后端 | 校验签名、解析 MIME、判断地址归属、落库；按账户类型选择发信通道 | 直接对外收发（它只跟 Worker 与中继说话） |
| 发信中继（Resend / SMTP） | 以本项目域名发信、DKIM 签名 | 收信 |
| 代理（Python） | SMTP/IMAP ↔ HTTP 的协议转换 | 存储、鉴权（都归后端） |

---

## 常见误解

**"本域地址是不是用了什么手段绕过了 QQ 邮箱的授权码？"**
没有。它压根没碰 QQ 邮箱。授权码是"以第三方客户端身份登录别人邮箱"的凭据，
而这条路径从头到尾没有登录过任何人的邮箱 —— 它只是让信送到本项目域名来。
两条路解决的是不同的问题，不是同一件事的两种做法。

**"配了 Cloudflare 就能发信了吗？"**
不能。收信和发信是两条独立配置的链路。只配了 Cloudflare 时，本域地址是
"能收不能发"的 —— 这是一个合理且常见的状态，界面上会明确标注出来。

**"换掉 `INBOUND_SHARED_SECRET` 会怎样？"**
两边必须完全一致。不一致时 Worker 会收到 401 并**退信**（而不是默默丢信），
所以问题会立刻以"发不进来"的形式暴露，而不是变成一封查无此信的神秘邮件。
