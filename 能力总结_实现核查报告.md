# 《能力总结.txt》实现情况核查报告

> 核查对象：`能力总结.txt` 中列出的 39 项能力点（后端 9 项 / 全栈 12 项 / Agent 18 项）
> 核查方式：逐项对照源码、SQL 建表脚本、依赖配置与前端页面，不修改任何业务代码
> 核查日期：2026-09-22
> 核查基准：`main` 分支 `c6666bf` + 工作区未提交改动

---

## 一、总体结论

| 结论 | 数量 | 占比 |
|------|------|------|
| ✅ 已实现 | 5 | 13% |
| ⚠️ 部分实现 | 10 | 26% |
| ❌ 未实现 | 24 | 61% |

**一句话结论**：本项目已完整实现的是**"站内邮件系统 + 基础智能插件 + 全栈 CRUD"**这一层——认证、邮件收发（站内）、附件存储、实时推送、分页、LLM 摘要调用都已跑通。但《能力总结》中占篇幅最大的 **Agent 工程化能力**（结构化输出、容错解析、多租户 BYO-Key、评测回归、成本监控、Prompt 工程）以及**协议层能力**（SMTP/IMAP）、**测试能力**、**管理员后台**几乎全部缺失。

**最大落差集中在三处**：
1. **Agent 章节 18 项中 12 项完全未实现**，且已实现的 LLM 部分只做"摘要"一件事，没有分类/垃圾识别/风险评分。
2. **智能分析的实际执行者是 Java 硬编码关键词规则**，不是 LLM；文档中"Python 规则插件""规则引擎接入 Java"在仓库中零代码。
3. **`llm_config` 是全局单行配置（id=1），不是账号级**，与"账号级 LLM 配置""多租户 BYO-Key""掩码回显"三项描述不符。

---

## 二、后端开发角度（9 项）

| # | 能力点 | 结论 | 证据 |
|---|--------|------|------|
| 1 | 认证/授权/JWT/Redis 黑名单 | ⚠️ 部分 | JWT 完整，黑名单与授权缺失 |
| 2 | SMTP 发信 / IMAP 收信 / 去重 | ❌ 未实现 | 无任何邮件协议依赖 |
| 3 | 智能分析（LLM、结构化输出、前端展示） | ⚠️ 部分 | LLM 只出摘要，无结构化输出 |
| 4 | 附件上传与存储 | ✅ 已实现 | MinIO + 本地磁盘双策略 |
| 5 | 登录审计 | ❌ 未实现 | 无表、无代码 |
| 6 | Python 规则插件 | ❌ 未实现 | 仓库零 `.py` 文件 |
| 7 | Redis 作为分析队列 | ❌ 未实现 | Redis 仅作缓存 |
| 8 | 自动化测试 | ❌ 未实现 | 无 `src/test` 目录 |
| 9 | 管理员后台 / 角色权限 | ❌ 未实现 | `user` 表无 role 字段 |

### 1. 认证/授权/JWT/Redis 黑名单 —— ⚠️ 部分实现

**已实现**：
- JWT 生成与校验：[JwtUtil.java](backend/src/main/java/com/mailsystem/util/JwtUtil.java) —— HS256 签名，claims 携带 `userId`/`email`，含过期时间。
- 全局拦截器：[JwtInterceptor.java](backend/src/main/java/com/mailsystem/config/JwtInterceptor.java#L22-L54) —— 从 `Authorization: Bearer` 提取并校验，注入 `userId` 到 request attribute。
- 注册/登录接口：[UserController.java](backend/src/main/java/com/mailsystem/controller/UserController.java#L28-L52)。
- 路由守卫 + token 注入（前端侧，见全栈章节）。

**缺失**：
- **Redis 黑名单完全没有**。全仓库检索 `blacklist` / `logout` 零命中；[JwtInterceptor.java](backend/src/main/java/com/mailsystem/config/JwtInterceptor.java) 只做签名与过期校验，没有查 Redis 吊销列表。
- **没有登出接口**。前端 [stores/user.js:32-37](frontend/src/stores/user.js#L32-L37) 的 `logout()` 只清 `localStorage`，**后端 token 在有效期内仍然可用**。
- **"授权"（角色/权限）不存在**。`user` 表（[init.sql:21-29](deploy/mysql/init.sql#L21-L29)）只有 `id/email/password/nickname/create_time`，无 `role` 字段；所有登录用户权限完全相同。
- 密码用 **SHA-256 无盐**哈希（[UserServiceImpl.java:59-67](backend/src/main/java/com/mailsystem/service/impl/UserServiceImpl.java#L59-L67)），非 BCrypt/Argon2，属弱口令存储。

### 2. SMTP 发信 / IMAP 收信 / 去重 —— ❌ 未实现

- `backend/pom.xml` 依赖清单中**无 `spring-boot-starter-mail`、`jakarta.mail`、`javax.mail`、`subethasmtp`** 等任何邮件协议库。
- 邮件"发送"仅向本地数据库插入记录：[MailServiceImpl.sendMail](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L69-L120)；收件人必须是本站注册用户，否则直接抛异常（[MailServiceImpl.java:573-584](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L573-L584) `throw new RuntimeException("用户不存在: " + email)`）。
- **去重逻辑零命中**（检索 `dedup` / `去重` / `duplicate` 无结果）。
- 补充说明：仓库存在 [ProtocolFilter.java](backend/src/main/java/com/mailsystem/config/ProtocolFilter.java) 与 `CustomProtocolEnvelope`，但那是把 HTTP 响应包成自定义 JSON 信封的开关式过滤器，**与 SMTP/IMAP 无关**，不能视为协议层实现。

### 3. 智能分析（LLM 调用、结构化输出、前端展示）—— ⚠️ 部分实现

- **LLM 调用 ✅**：[LlmPlugin.java](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java) 是真实可用的 LLM 接入，支持 OpenAI Chat Completions 与 Anthropic Messages 两种格式自动分流（[:106-115](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L106-L115)），配置 30 秒缓存（[:36-38](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L36-L38)）。
- **结构化输出 ❌**：LLM 被要求"只返回摘要内容"（[:64-66](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L64-L66)），返回自由文本直接写库（[:76](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L76)），**无 JSON Schema、无 json_object 模式、无字段校验**。
- **邮件分类 / 垃圾邮件识别 ⚠️（由 Java 规则而非 LLM 完成）**：
  - 垃圾识别：[SpamFilterPlugin.java:24-89](backend/src/main/java/com/mailsystem/plugin/SpamFilterPlugin.java#L24-L89) —— 25 个硬编码关键词 + 可疑域名 + 长度/大小写/感叹号启发式，阈值 30 分。
  - 分类：[CategoryPlugin.java:24](backend/src/main/java/com/mailsystem/plugin/CategoryPlugin.java#L24) —— 硬编码 `CATEGORY_RULES` 关键词二维数组。
  - 即"智能分析"的实际智能来自规则匹配，LLM 仅补充摘要。
- **前端展示 ✅（较完整）**：[MailDetail.vue:28-57](frontend/src/views/MailDetail.vue#L28-L57) 展示高优先级标签、垃圾邮件标签、智能分类标签、AI 摘要；[Inbox.vue:63-72](frontend/src/views/Inbox.vue#L63-L72) 列表页展示"重要/可疑/分类"标签。

### 4. 附件上传与存储 —— ✅ 已实现

- 上传/下载/预览三个接口齐备：[AttachmentController.java](backend/src/main/java/com/mailsystem/controller/AttachmentController.java#L29-L75)（`/upload`、`/download/{id}`、`/preview/{id}`）。
- **双存储策略**且实现了自动降级：[AttachmentServiceImpl.java:38-43](backend/src/main/java/com/mailsystem/service/impl/AttachmentServiceImpl.java#L38-L43) —— MinIO 优先，未启用时回退本地磁盘；读取时按路径前缀自动判别（[:80-84](backend/src/main/java/com/mailsystem/service/impl/AttachmentServiceImpl.java#L80-L84)）。
- 附件随转发复制：[MailServiceImpl.java:551-565](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L551-L565)。
- 这是全仓库完成度最高的模块之一。

### 5. 登录审计 —— ❌ 未实现

- 检索 `audit` / `审计` / `login_log` / `loginlog` 在 `backend/src`、`deploy`、`frontend/src` 中**零命中**。
- 无审计表，[UserController.login](backend/src/main/java/com/mailsystem/controller/UserController.java#L42-L52) 中无任何登录事件记录（IP、UA、时间、成功/失败）落库或落日志。
- 仅有 `System.err.println` 级别的散落错误打印，不构成审计。

### 6. Python 规则插件 —— ❌ 未实现

- 全仓库 `find -name "*.py"` **返回空**。
- 规则引擎确实存在，但**全部是 Java 实现**：[SpamFilterPlugin.java](backend/src/main/java/com/mailsystem/plugin/SpamFilterPlugin.java)、[CategoryPlugin.java](backend/src/main/java/com/mailsystem/plugin/CategoryPlugin.java)、[PrioritySortPlugin.java](backend/src/main/java/com/mailsystem/plugin/PrioritySortPlugin.java)、[LinkDetectionPlugin.java](backend/src/main/java/com/mailsystem/plugin/LinkDetectionPlugin.java)、[SummaryPlugin.java](backend/src/main/java/com/mailsystem/plugin/SummaryPlugin.java)。
- 与文档描述的"Python 规则插件"存在**技术栈层面的根本不符**。

### 7. Redis 作为分析队列 —— ❌ 未实现

- 检索 `lpush` / `rpush` / `@RabbitListener` / `kafka` / `队列` **零命中**。
- Redis 的实际用途只有两处：**列表页缓存**（[MailServiceImpl.java:171](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L171)，5 分钟过期）与**未读数缓存**（[:288-297](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L288-L297)，2 分钟过期）。
- 插件执行是**进程内同步 for 循环**：[MailServiceImpl.executePlugins:636-648](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L636-L648)，逐个 `plugin.process(mail)`。`LlmPlugin` 上有 `@Async`（[LlmPlugin.java:46-47](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L46-L47)）属线程池异步，**不经过 Redis 队列**，且无重试、无死信、无削峰。

### 8. 自动化测试 —— ❌ 未实现

- **不存在 `backend/src/test` 目录**，无任何 `*Test.java` 文件。
- `backend/pom.xml:109` 声明了 `spring-boot-starter-test` 依赖，但**没有任何测试代码使用它**。
- 前端 `package.json` 的 `devDependencies` 只有 `@vitejs/plugin-vue` 与 `vite`，**无 vitest / jest / cypress / playwright**，`scripts` 中无 `test` 命令。
- 结论：测试依赖"留了口子"，测试本身"一行未写"。

### 9. 管理员后台 / 角色权限 —— ❌ 未实现

- `user` 表无 `role` / `is_admin` 字段（[init.sql:21-29](deploy/mysql/init.sql#L21-L29)）。
- 无 `AdminController`、无 `admin` 路由、无任何管理后台页面。检索 `admin` 仅命中 `application.yml` 中的 MinIO 账号名与 `MinioConfig.java`，**均为对象存储配置，非权限系统**。
- 接口无角色校验，例如任意登录用户都可以改全局 LLM 配置（见下方 Agent 章节第 7 项，这同时是一个越权问题）。

---

## 三、全栈开发角度（12 项）

| # | 能力点 | 结论 | 证据 |
|---|--------|------|------|
| 1 | 登录/注册、路由守卫、token 注入 | ✅ 已实现 | 三件套完整 |
| 2 | 邮件列表/详情/筛选（已读、优先级、风险、关键词） | ⚠️ 部分 | 筛选控件缺失 |
| 3 | 侧栏文件夹、多账号切换、暗黑模式、骨架屏 | ⚠️ 部分 | 仅文件夹 |
| 4 | 写邮件/存草稿、联系人管理、推送事件弹窗 | ⚠️ 部分 | 无联系人 |
| 5 | AI 分析结果可视化 | ⚠️ 部分 | 仅标签+文本 |
| 6 | 账号级 LLM 配置（自带 Key、掩码回显） | ❌ 未实现 | 全局单行配置 |
| 7 | 邮件列表分页 | ✅ 已实现 | 四个列表页均有 |
| 8 | refresh token 自动续期 | ❌ 未实现 | 无 refresh 机制 |
| 9 | 实时推送（SSE/WebSocket） | ✅ 已实现 | STOMP over SockJS |
| 10 | 富文本编辑器 | ❌ 未实现 | 纯 textarea |
| 11 | 前端自动化测试 | ❌ 未实现 | 无测试框架 |
| 12 | 管理员后台页面 | ❌ 未实现 | 无页面 |

### 1. 登录/注册、路由守卫、token 注入 —— ✅ 已实现

- 页面：[Login.vue](frontend/src/views/Login.vue)、[Register.vue](frontend/src/views/Register.vue)。
- 路由守卫：[router/index.js:73-91](frontend/src/router/index.js#L73-L91) —— `requiresAuth` 校验，未登录跳转并携带 `redirect`；已登录访问登录/注册页反向跳转首页。
- token 注入：[api/request.js:10-19](frontend/src/api/request.js#L10-L19) —— 请求拦截器统一加 `Authorization: Bearer`。
- 响应拦截：401 统一清登录态并跳登录页（[request.js:41-45](frontend/src/api/request.js#L41-L45)）。
- 可作为全栈部分的标杆实现。

### 2. 邮件列表/详情/筛选（已读、优先级、风险、关键词）—— ⚠️ 部分实现

**已实现**：
- 列表/详情：`/mail/list`（[MailController.java:65](backend/src/main/java/com/mailsystem/controller/MailController.java#L65)）、`/mail/detail/{id}`（[:79](backend/src/main/java/com/mailsystem/controller/MailController.java#L79)）。
- **关键词**：搜索框 + `/mail/search`（[Inbox.vue:21-29](frontend/src/views/Inbox.vue#L21-L29)，后端 [MailServiceImpl.searchMails:278-285](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L278-L285)），按主题/正文模糊匹配。
- **已读**：单封切换（[Inbox.vue:49-58](frontend/src/views/Inbox.vue#L49-L58)）+ 后端 `toggleRead` 三接口。
- **优先级 / 风险（垃圾）**：**仅以标签形式展示**，非筛选维度。

**缺失**：
- **没有任何筛选控件**。[Inbox.vue](frontend/src/views/Inbox.vue) 工具栏只有"写信/刷新/批量删除 + 搜索框"，**没有"只看未读""只看高优先级""只看风险邮件"的下拉或多选**；后端 `/mail/list` 也不接收 priority/risk/isRead 过滤参数，仅按文件夹类型 + 分页。
- 因此"筛选（已读、优先级、风险、关键词）"中的**关键词 ✅、已读 ⚠️（仅单封操作）、优先级 ❌、风险 ❌**。

### 3. 侧栏文件夹、多账号切换、暗黑模式、骨架屏 —— ⚠️ 部分实现（4 项中 1 项）

| 子项 | 结论 | 证据 |
|------|------|------|
| 侧栏文件夹 | ✅ | [MainLayout.vue:42-62](frontend/src/layouts/MainLayout.vue#L42-L62) —— 收件箱/已发送/草稿箱/垃圾箱/设置 |
| 多账号切换 | ❌ | 检索 `多账号`/`account` 零命中；单 token 单身份 |
| 暗黑模式 | ❌ | 检索 `dark`/`theme` 仅命中 `effect="dark"`（标签视觉属性，非主题） |
| 骨架屏 | ❌ | 检索 `el-skeleton`/`skeleton` 零命中；列表加载用 `v-loading` 转圈 |

### 4. 写邮件/存草稿、联系人管理、推送事件弹窗 —— ⚠️ 部分实现（3 项中 2 项）

| 子项 | 结论 | 证据 |
|------|------|------|
| 写邮件/存草稿 | ✅ | [Compose.vue](frontend/src/views/Compose.vue) + [Drafts.vue](frontend/src/views/Drafts.vue)；后端草稿四接口（save/update/send/delete），草稿附件重绑定见 [MailServiceImpl.java:397-401](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L397-L401) |
| 联系人管理 | ❌ | 检索 `contact`/`联系人`/`addressbook` 零命中；无表、无页面、无接口，收件人需手输邮箱 |
| 推送事件弹窗 | ✅ | [useWebSocket.js:71-73](frontend/src/composables/useWebSocket.js#L71-L73) `case 'NEW_MAIL'` → `ElNotification` |

### 5. AI 分析结果可视化 —— ⚠️ 部分实现

- 已有的是**标签 + 纯文本**：[MailDetail.vue:28-57](frontend/src/views/MailDetail.vue#L28-L57) 用 `el-tag` 展示"高优先级/垃圾邮件/智能分类"，摘要以斜体灰字一行显示。
- **没有"可视化"层面的呈现**：无风险分数量化展示、无置信度、无 indicators 命中明细、无 actions 建议列表、无图表/仪表盘。
- 根因在后端：LLM 只返回一段摘要文本，`mail` 表也只有 `summary/priority/is_spam/category` 四个标量列（[init.sql:44-47](deploy/mysql/init.sql#L44-L47)），**没有可供可视化的结构化数据**。

### 6. 账号级 LLM 配置（自带 Key、掩码回显）—— ❌ 未实现

**最需要修正的一项描述。** 三层均不满足：

1. **不是账号级，是全局单行**。`llm_config` 表主键 `id`，**代码写死 `WHERE id = 1`**（[PluginServiceImpl.java:51-52](backend/src/main/java/com/mailsystem/service/impl/PluginServiceImpl.java#L51-L52)、[:106](backend/src/main/java/com/mailsystem/service/impl/PluginServiceImpl.java#L106)），**没有 `user_id` 字段**。所有用户共用同一把 Key，且 `LlmPlugin` 读配置时不带用户上下文（[LlmPlugin.java:225-237](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L225-L237)）。
2. **越权隐患**。`/plugin/llm/config`（GET）与 `/plugin/llm/configure`（PUT）（[PluginController.java:53-73](backend/src/main/java/com/mailsystem/controller/PluginController.java#L53-L73)）只需登录即可访问，**任意普通用户都能读取和覆盖他人的 API Key**。
3. **不是掩码回显**。`getLlmConfig` 原样返回明文 `apiKey`（[PluginServiceImpl.java:59](backend/src/main/java/com/mailsystem/service/impl/PluginServiceImpl.java#L59)），前端直接填入输入框（[Settings.vue:131](frontend/src/views/Settings.vue#L131)），控件仅靠 `type="password" show-password`（[Settings.vue:44](frontend/src/views/Settings.vue#L44)）——**点一下眼睛就明文可见**，且 Key 每 30 秒从 DB 明文读出。这是"输入框遮挡"，不是"掩码回显"。

**符合文档描述的部分**：确实提供了 `apiEndpoint` / `apiKey` / `modelName` 三个字段的自助配置页面。

### 7. 邮件列表分页 —— ✅ 已实现

- 四个列表页均接入 `el-pagination`：[Inbox.vue:84-92](frontend/src/views/Inbox.vue#L84-L92)、[Sent.vue:37](frontend/src/views/Sent.vue#L37)、[Drafts.vue:48](frontend/src/views/Drafts.vue#L48)、[Trash.vue:57](frontend/src/views/Trash.vue#L57)。
- 后端 MyBatis-Plus 分页 + `PageResult` 统一封装（[MailServiceImpl.listMails:132-174](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L132-L174)）。

### 8. refresh token 自动续期 —— ❌ 未实现

- **无 refresh token 概念**：`LoginResponse` 只返回单个 `token`；`JwtUtil` 只生成一种 token（[JwtUtil.java:26-38](backend/src/main/java/com/mailsystem/util/JwtUtil.java#L26-L38)）。
- **无续期机制**：[request.js](frontend/src/api/request.js) 无响应拦截重试/刷新逻辑，**401 直接清空登录态跳登录页**（[:41-45](frontend/src/api/request.js#L41-L45)），用户被强制重新登录。
- **无滑动过期**：token 过期时间在签发时固定（`jwt.expiration`），中途无续期接口。

### 9. 实时推送（SSE/WebSocket）—— ✅ 已实现

- 采用 **STOMP over WebSocket**：[useWebSocket.js:28-40](frontend/src/composables/useWebSocket.js#L28-L40)，SockJS 降级 + 订阅 `/topic/user/${userId}`。
- 后端：[WebSocketConfig.java](backend/src/main/java/com/mailsystem/config/WebSocketConfig.java) + [MailNotificationService.java](backend/src/main/java/com/mailsystem/websocket/MailNotificationService.java)。
- 推送场景覆盖：新邮件（[MailServiceImpl.java:567-571](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L567-L571)）、已读回执（[:205-208](backend/src/main/java/com/mailsystem/service/impl/MailServiceImpl.java#L205-L208)）。
- **额外加分项**：WebSocket 不可用时自动降级轮询（[useWebSocket.js:55-64](frontend/src/composables/useWebSocket.js#L55-L64)），健壮性设计到位。

### 10. 富文本编辑器 —— ❌ 未实现

- [Compose.vue:28-35](frontend/src/views/Compose.vue#L28-L35) 正文是 `el-input type="textarea"` —— **纯文本域**，仅外层有个名为 `editor-wrapper` 的 CSS 类，无编辑器语义。
- `package.json` **无 quill / tinymce / wangeditor / tiptap** 等任何富文本依赖；全仓库检索 `contenteditable` 零命中。
- 因此加粗/列表/图片/超链接等能力均不存在。附带影响：`mail.body` 是无格式纯文本（`LONGTEXT`），LLM 的"HTML 剥离"环节也因此无 HTML 可剥（见 Agent 章节第 5 项）。

### 11. 前端自动化测试 —— ❌ 未实现

- 与后端测试同理：`frontend/package.json` 无 vitest/jest/cypress/playwright，无 `test` 脚本，无任何 `*.spec.js` / `*.test.js` 文件。

### 12. 管理员后台页面 —— ❌ 未实现

- [router/index.js](frontend/src/router/index.js) 路由表中**无 `/admin` 相关条目**，`MainLayout.vue` 侧栏无管理入口。
- 与后端章节第 9 项对应，前后端管理员能力整体缺失。

---

## 四、Agent 开发角度（18 项）

| # | 能力点 | 结论 |
|---|--------|------|
| 1 | LLM 接入 | ✅ 已实现 |
| 2 | Prompt 设计（角色/任务/Schema/枚举约束） | ⚠️ 部分 |
| 3 | 结构化输出四级容错 | ❌ 未实现 |
| 4 | 防御式解析（path + 默认值 + clamp） | ❌ 未实现 |
| 5 | 上下文控制（HTML 剥离 + 3000 截断 + 链接抽取） | ⚠️ 部分 |
| 6 | 输出契约（spam/priority/risk + indicators + actions） | ❌ 未实现 |
| 7 | 多租户 BYO-Key（baseUrl/apiKey/model + 掩码） | ⚠️ 部分 |
| 8 | Python 规则引擎代码 | ❌ 未实现 |
| 9 | 规则引擎接入 Java（native / JNA） | ❌ 未实现 |
| 10 | 结果持久化（`mail_intelligence_result` 表） | ❌ 未实现 |
| 11 | Redis 分析队列 | ❌ 未实现 |
| 12 | 评测集 / 指标 / 回归测试 | ❌ 未实现 |
| 13 | Prompt 版本管理 | ❌ 未实现 |
| 14 | LLM 调用超时 / 熔断 / 限流 | ❌ 未实现 |
| 15 | Prompt Injection 防护 | ❌ 未实现 |
| 16 | Token 成本与延迟监控 | ❌ 未实现 |
| 17 | 人工反馈回流 | ❌ 未实现 |
| 18 | 工具调用 / 多步 Agent | ❌ 未实现 |

> **本章节 18 项中，1 项完整、4 项部分、13 项完全未实现。** 这是文档与实际差距最大的部分。

### 1. LLM 接入 —— ✅ 已实现

- [LlmPlugin.java](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java) 是真实可运行的 LLM 客户端，非占位代码。
- **双协议自动分流**（较亮眼）：按 endpoint 主机名判 `*.anthropic.com` 走 Messages API，否则走 OpenAI Chat Completions（[:106-115](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L106-L115)），两套请求/响应解析分别落地在 [:119-206](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L119-L206)。
- URL 拼接有防重复处理（`buildApiUrl`，[:214-220](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L214-L220)）。
- 配置读取有 30 秒缓存，避免每封邮件查库（[:35-38](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L35-L38)）。
- 异常被捕获降级，不阻断主流程（[:81-83](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L81-L83)）。
- 工作区有一处未提交的小修：`new java.net.URL(...)` → `URI.create(...)` 以消除 URL 构造器的安全告警（`git diff` 确认，2 行改动，不影响功能）。

### 2. Prompt 设计（角色/任务/Schema/枚举约束）—— ⚠️ 部分实现

实际的 Prompt 只有一处，硬编码在 [LlmPlugin.java:64-66](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L64-L66)：

```
你是一个邮件摘要助手。请用一句话总结以下邮件内容（不超过200字），
只返回摘要内容，不要加任何前缀、后缀或额外说明。
如果是中文邮件用中文回复，英文邮件用英文回复。
```

对照四个要素：

| 要素 | 状态 | 说明 |
|------|------|------|
| 角色 | ✅ | "你是一个邮件摘要助手" |
| 任务 | ✅ | 一句话总结，≤200 字 |
| Schema | ❌ | 无 JSON 结构说明，纯自然语言输出 |
| 枚举约束 | ❌ | 无分类枚举、无风险等级枚举 |

**结论**：具备最基础的角色+任务设定，但**Schema 与枚举约束完全缺失**。同时 Prompt 是**代码内硬编码常量**，无法在运行时调整或做 A/B，这也是第 13 项"Prompt 版本管理"缺失的直接体现。

### 3. 结构化输出四级容错（json_object → 重试 → 剥壳 → 默认值）—— ❌ 未实现

四级机制**逐级核查均为零**：

| 级别 | 检索 | 结果 |
|------|------|------|
| json_object 模式 | `json_object` / `response_format` | 零命中，请求体只有 `model`/`messages`/`max_tokens`/`temperature`（[LlmPlugin.java:124-140](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L124-L140)） |
| 失败重试 | `RetryTemplate` / 重试计数 | 零命中；`:81-83` 直接 catch 后 return |
| 剥壳（去 ```json 包裹） | `json` / `trim` / 正则提取 | 仅有 `summary.trim()`（[:76](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L76)），非 JSON 剥壳 |
| 默认值兜底 | 默认对象 / 缺省字段 | 零命中 |

根因：项目**从未要求 LLM 输出结构化数据**，因此容错链无需存在。这是"因功能缺失导致的容错缺失"。

### 4. 防御式解析（path + 默认值 + clamp）—— ❌ 未实现

- 现有解析是**直接强转 + 假设路径必然存在**，例如 `callOpenAiApi` 中逐层 `get` 后转 `List<Map<String,Object>>`（[:152-161](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L152-L161)），仅靠外层 `try/catch` 整体兜底。
- **无按路径取值的安全访问器**、**无字段级默认值**、**无数值 clamp**（clamp 的典型场景——优先级 0-100、风险分区间——在本项目中由 Java 规则的 `updatePriority` 自行保证，不在 LLM 侧）。
- 与第 3 项同源：无结构化输出，自然无防御式解析。

### 5. 上下文控制（HTML 剥离 + 3000 字符截断 + 链接抽取）—— ⚠️ 部分实现

| 子项 | 状态 | 证据 |
|------|------|------|
| HTML 剥离 | ❌ | 检索 `stripHtml`/`sanitize`/`HTML剥离` 零命中；正文直接拼接进 Prompt（[:135](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L135)）。**缓解因素**：正文来自 textarea 纯文本（见全栈第 10 项），当前无 HTML 可剥 |
| 字符截断 | ⚠️ 阈值不符 | 截断**存在但阈值是 4000 而非 3000**：[LlmPlugin.java:122](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L122) 与 [:170](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L170) 均为 `content.length() > 4000 ? substring(0, 4000)` |
| 链接抽取 | ⚠️ 有实现但不在 LLM 链路 | [LinkDetectionPlugin.java](backend/src/main/java/com/mailsystem/plugin/LinkDetectionPlugin.java) 做短链域名与可疑 TLD 检测（[:36-42](backend/src/main/java/com/mailsystem/plugin/LinkDetectionPlugin.java#L36-L42)），但那是**独立插件**，抽取结果**不喂给 LLM**，两者互不感知 |

### 6. 输出契约（spam/priority/risk + indicators + actions）—— ❌ 未实现

- 检索 `indicators` / `actions` / `intelligence` / `risk` 在 `backend/src`、`frontend/src`、`deploy` 中**零命中**。
- 实际的"输出契约"是**五个标量列**：`mail.priority`（INT）、`mail.is_spam`（TINYINT）、`mail.summary`（VARCHAR）、`mail.category`（VARCHAR）（[init.sql:44-47](deploy/mysql/init.sql#L44-L47)）。
- **没有任何契约层面的约定**：无 spam/priority/risk 三元组、无 `indicators`（命中指标明细数组）、无 `actions`（处置建议数组）。
- 这是第 3、4 项缺失的**共同上游原因**——没有契约，就不需要结构化解析与容错。

### 7. 多租户 BYO-Key（自带 baseUrl/apiKey/model + 掩码）—— ⚠️ 部分实现

| 子项 | 状态 | 说明 |
|------|------|------|
| baseUrl / apiKey / model 可配 | ✅ | 三字段齐备，[Settings.vue:40-48](frontend/src/views/Settings.vue#L40-L48) + [PluginController.java:64-73](backend/src/main/java/com/mailsystem/controller/PluginController.java#L64-L73) |
| 多租户（按账号隔离） | ❌ | `llm_config` 无 `user_id`，代码写死 `id = 1`，全局唯一 |
| 掩码 | ❌ | 明文返回 + `show-password` 可一键显形 |
| 越权控制 | ❌ | 任何登录用户可读写全局 Key（详见全栈第 6 项分析） |

**结论**：做到了"BYO-Key"的**字段层**，完全没做到"多租户"与"掩码"。

### 8. Python 规则引擎代码 —— ❌ 未实现

- 同后端章节第 6 项：仓库 `.py` 文件数为 **0**。

### 9. 规则引擎接入 Java（native 动态库 / JNA）—— ❌ 未实现

- 检索 `JNA` / `System.loadLibrary` / `native` 在 `backend/src` 零命中；`pom.xml` 无 JNA/JNI 相关依赖。
- 现状是**纯 Java 规则**（关键词数组 + 启发式打分），没有跨语言调用，也就没有"接入"这一层。第 8、9 项实为**同一设计意图的两半，两半都不存在**。

### 10. 结果持久化（`mail_intelligence_result` 表）—— ❌ 未实现

- **该表不存在**。`deploy/mysql/` 三个脚本中无此表（[init.sql](deploy/mysql/init.sql)、`migration_v2.sql`、`clear_data.sql`）。
- 全仓库检索 `mail_intelligence_result` **零命中**。
- 实际持久化方式是**把分析结果扁平化写进 `mail` 表的四个标量列**（`priority`/`is_spam`/`summary`/`category`），通过 [MailMapper](backend/src/main/java/com/mailsystem/mapper/MailMapper.java#L104-L122) 的四个 `updateXxx` 方法实现。
- 该设计的**直接局限**：无法存储 indicators/actions 等明细、无法保留多次分析的历史版本、无法区分是哪个规则/模型产出的结果、无法做分析结果审计。

### 11. Redis 分析队列 —— ❌ 未实现

- 同后端章节第 7 项：Redis 只作缓存，插件为进程内同步循环。

### 12. 评测集 / 指标 / 回归测试 —— ❌ 未实现

- 检索 `eval` / `评测` / `metric` / `regression` **零命中**。
- 无标注邮件样本集、无准确率/召回率/F1 计算、无模型或 Prompt 变更后的回归对比。结合第 8 项（无测试），**整个项目没有任何质量度量设施**。

### 13. Prompt 版本管理 —— ❌ 未实现

- Prompt 以**字符串常量硬编码**在 [LlmPlugin.java:64-66](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L64-L66) 的方法体内。
- 无 Prompt 表、无版本号字段、无变更记录、无灰度/回滚能力。改动 Prompt 必须改代码并重新构建镜像。

### 14. LLM 调用超时 / 熔断 / 限流 —— ❌ 未实现

- **超时**：[LlmPlugin.java:33](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L33) `new RestTemplate()` 使用 JDK 默认 **无限超时**（无 connect/read timeout 设置）。检索 `setConnectTimeout`/`setReadTimeout` 零命中。**这是实际风险点**：LLM 端挂起会持续占用 `@Async` 线程池线程，累积可致队列堆积。
- **熔断**：无（检索 `circuit`/`Resilience4j`/`Hystrix` 零命中）。
- **限流**：无（检索 `ratelimit`/`RateLimiter` 零命中）；`max_tokens=300` 是**单次输出上限**，不是调用频率限制。

### 15. Prompt Injection 防护 —— ❌ 未实现

- 邮件正文**未经任何处理直接拼入 user message**（[LlmPlugin.java:135](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L135)、[:180](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L180)），发件人可完全控制该内容。
- 无输入过滤、无分隔符/定界标记、无"忽略正文中的指令"之类的系统级声明。
- **缓解因素（但非防护）**：当前 LLM 只做摘要、输出不参与任何控制流判定，注入的破坏面有限。**若按文档补齐分类/风险判定并让结果驱动业务动作，此缺口将立刻升级为高危**——注入可操纵垃圾判定与风险评分。

### 16. Token 成本与延迟监控 —— ❌ 未实现

- 检索 `token` / `usage` / `latency` / `cost` **零命中**。
- 请求未记录调用耗时；响应中的 `usage` 字段（OpenAI/Anthropic 均返回 prompt/completion tokens）**被完全忽略**，解析只取 `choices[0].message.content`（[:152-161](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L152-L161)）或 `content[0].text`（[:196-204](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L196-L204)）。
- 无调用量统计、无费用估算、无 P95 延迟、无失败率看板。

### 17. 人工反馈回流 —— ❌ 未实现

- 检索 `feedback` / `反馈` / `thumbs` / `rating` **零命中**。
- 前端**无任何"分析结果对不对"的反馈入口**（[MailDetail.vue](frontend/src/views/MailDetail.vue) 只展示标签）；后端无反馈表、无接口。
- 因此不存在"用户纠正 → 数据沉淀 → 改进规则/Prompt"的闭环。

### 18. 工具调用 / 多步 Agent —— ❌ 未实现

- 检索 `tool_call` / `functionCall` / `agent` / `多步` 在 `backend/src` **零命中**。
- 所有 LLM 交互均为**单轮、无工具、无循环**：一次请求 → 取一段文本 → 写库（[LlmPlugin.process:47-84](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L47-L84)）。
- 无 function calling、无 ReAct 循环、无多步规划。当前架构是**"LLM 作为文本生成器"**，尚未进入 Agent 范式。

---

## 五、关键落差与风险汇总

### 5.1 与文档描述存在的技术栈级不符（需优先澄清）

| 文档描述 | 仓库实际 | 影响 |
|----------|----------|------|
| Python 规则插件 / Python 规则引擎代码 | **纯 Java 规则**，零 `.py` 文件 | 语言栈根本不符，非"未完成"而是"未采用该方案" |
| 规则引擎接入 Java（native / JNA） | **无跨语言调用**，规则本就在 Java 内 | 该设计意图不成立 |
| SMTP 发信 / IMAP 收信 | **纯站内邮件**，收件人须为注册用户 | 系统不具备与外部邮箱通信能力 |
| `mail_intelligence_result` 表 | **表不存在**，结果扁平写入 `mail` 表四列 | 无法存明细/历史，限制后续所有扩展 |
| 账号级 LLM 配置 / 多租户 BYO-Key | **全局单行配置（id=1）** | 与"多租户"语义相反 |
| 掩码回显 | **明文返回 + show-password 可显形** | 属"输入框遮挡"而非掩码 |
| Redis 分析队列 | **Redis 仅作缓存** | 队列能力不存在 |
| 富文本编辑器 | **纯 textarea** | 也因此没有 HTML 需要剥离 |

### 5.2 需要关注的安全问题（附带发现，非文档要求）

1. **越权读取/覆盖 API Key**：`/plugin/llm/config` 与 `/plugin/llm/configure` 无角色校验，任意登录用户可读改全局 LLM 密钥（[PluginController.java:53-73](backend/src/main/java/com/mailsystem/controller/PluginController.java#L53-L73)）。
2. **API Key 明文落库与明文回传**：`llm_config.api_key` 明文存储（[init.sql:117](deploy/mysql/init.sql#L117)），接口原样返回。建议加密存储 + 返回时替换为掩码（如 `sk-****1234`），保存时空值表示"不修改"。
3. **登出无法吊销 token**：登出仅清前端 localStorage，token 在剩余有效期内仍可用；无黑名单即无强制下线能力。
4. **密码哈希无盐**：SHA-256 单次哈希（[UserServiceImpl.java:59-67](backend/src/main/java/com/mailsystem/service/impl/UserServiceImpl.java#L59-L67)），易受彩虹表与 GPU 暴力破解，建议换 BCrypt/Argon2。
5. **LLM 调用无超时**：`new RestTemplate()` 默认无限超时，上游挂起会持续占用异步线程池（[LlmPlugin.java:33](backend/src/main/java/com/mailsystem/plugin/llm/LlmPlugin.java#L33)）。

### 5.3 完成度评价

按文档三个角度的**加权完成度**（以条目数为权重，⚠️ 按 0.5 计）：

| 章节 | 条目 | ✅ | ⚠️ | ❌ | 折算完成度 |
|------|------|----|----|----|-----------|
| 后端开发 | 9 | 1 | 2 | 6 | **22%** |
| 全栈开发 | 12 | 3 | 4 | 5 | **42%** |
| Agent 开发 | 18 | 1 | 4 | 13 | **17%** |
| **合计** | **39** | **5** | **10** | **24** | **26%** |

**结构性观察**：完成度呈现"**应用层 > 全栈层 > Agent 层**"的阶梯下降。项目实质上是一个**功能完整、工程质量尚可的站内邮件系统**，其"智能"部分由 Java 规则承担，LLM 仅作为摘要文本生成器被接入；而《能力总结》中占 46%（18/39）篇幅的 Agent 工程化能力，绝大多数停留在规划阶段。

**建议**：若该文档用于对外陈述能力，建议按"已实现 / 进行中 / 规划中"三档重新标注，特别是上文 5.1 表格中的 8 处技术栈级不符需优先修正，避免能力描述与实际交付物出现可被直接验证的偏差。

---

## 六、核查方法说明

- **代码检索**：对 `backend/src`、`frontend/src`、`deploy` 全量检索关键词（`audit`/`dedup`/`queue`/`ratelimit`/`circuit`/`eval`/`injection`/`indicators`/`actions`/`intelligence`/`tool_call`/`feedback`/`contact`/`skeleton`/`dark`/`admin` 等），零命中即判定"未实现"。
- **文件级确认**：`find -name "*.py"` 确认无 Python；确认无 `src/test` 目录；确认无任一前端测试框架依赖。
- **依赖确认**：`backend/pom.xml` 全文比对，确认无 `spring-boot-starter-mail`/`javax.mail`/JNA。
- **表结构确认**：逐一比对 `deploy/mysql/` 下三个 SQL 脚本的建表语句。
- **本次核查未修改任何业务代码**，仅新增本报告文件。
- 报告中所有代码位置均为 `文件:行号`，可逐条复查。
